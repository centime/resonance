todos :

ff-irc :
    [Exception... "'Error: Uncaught, unspecified 'error' event.' when calling method: [nsIStreamListener::onDataAvailable]"
    des que IRC lui dit un truc qu'il faut pas (nick utilisé, pm à qqn qui n'existe pas...)
    ssl

interface
    resize (via js)

topPages :
    si le client ouvre 2 fois la meme page, il ne doit s'annoncer / se retirer qu'une fois
    comment savoir quand le client ferme brutalement son navigateur ?
    on('close')
    interface
    filtres, categories

workers & chans :
    2 onglets sur la même page ?

admin-bot :
    doit verifier que la personne est venue dans le cchan avant d'en sortir
    connection en ssl
    nickserv
    optimisation temps de traitement, taille de la liste.

messages :
    refactor on('message') et on('pm')

users :
    /nick zobizob
    par liste alphabetique
    clickable, avec menu d'options

mute :
    clignotement quand il parle

encapsulation http
    pour bypass les restrictions de ports des reseaux publiques

no-track :
    desactiver /whois : ok avec le mode +i
    comment faire pour ne pas pouvoir tracker, NOUS, les utilisateurs.
    limiter le nombre de canaux simultanés ?

refactor main.js

systeme de mise a jour apres installation

clean disconnect