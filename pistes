tabs.on("ready", function(tab) {
  console.log(tab.title);
});
tabs.on("close", function(tab) {
  console.log(tab.title);
});
+ pour savoir de quelle page il part, reperer a quel worker ca correspond grace à worker.tab (https://developer.mozilla.org/en-US/Add-ons/SDK/Low-Level_APIs/content_worker)


ou :
"Note that tab.attach is tab-centric: if the user navigates to a new page in the same tab, then the worker and content scripts will be reattached to the new page."
https://developer.mozilla.org/en-US/Add-ons/SDK/High-Level_APIs/tabs